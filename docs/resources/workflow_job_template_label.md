---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "awx_workflow_job_template_label Resource - awx"
subcategory: ""
description: |-
  Associate label(s) to a workflow job template.
---

# awx_workflow_job_template_label (Resource)

Associate label(s) to a workflow job template.

## Example Usage

```terraform
resource "awx_organization" "example" {
  name = "example-organization"
}

resource "awx_workflow_job_template" "example" {
  name         = "example-workflow-job-template"
  organization = awx_organization.example.id
}

resource "awx_label" "example-1" {
  name         = "example-label-1"
  organization = awx_organization.example.id
}

resource "awx_label" "example-2" {
  name         = "example-label-2"
  organization = awx_organization.example.id
}

resource "awx_workflow_job_template_label" "example" {
  label_ids                = [awx_label.example-1.id, awx_label.example-2.id]
  workflow_job_template_id = awx_workflow_job_template.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `label_ids` (Set of Number) An unordered list of label IDs associated to a particular Wokflow Job Template. Create new labels first with `Automation Controller_label` resource type.
- `workflow_job_template_id` (String) The ID of the containing Wokflow Job Template.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import awx_workflow_job_template_label.example 100
```
